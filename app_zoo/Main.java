package app_zoo;

public class Main {
    public static void main(String[] args) {
     Controller.start();
     //Action.show();
/*      Scanner caner =new Scanner(System.in);
     String tst = Action.show();
    System.out.println(tst);
    System.out.println("Введите новую команду из списка " + Action.show());
    while (true) {
        String newCommand = caner.nextLine();
        if (Action.find(newCommand)) {
            System.out.println("добавлено");
        } else {
            System.out.println("недоступная команда");
        }
    }
} */
    }
 

}
